package main

import (
	"fmt"
	"strings"
)

func main() {
	x := "HELLO HII EVERYONE OOD EVENING"
	y := "MAP IS A KEY VALUE TYPE"
	z := "STRUCTURE IS A USER DEFINED DATATYPE"
	res1 := strings.ToLower(x)
	res2 := strings.ToLower(y)
	res3 := strings.ToLower(z)
	fmt.Println(res1)
	fmt.Println(res2)
	fmt.Println(res3)
}
